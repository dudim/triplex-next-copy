/** Типы компонента SegmentedControl. */
export enum ESegmentedControlType {
    SINGLE = "single",
    MULTIPLE = "multiple",
}

/** Темы компонента SegmentedControl. */
export enum ESegmentedControlTheme {
    GENERAL_1 = "general_1",
    GENERAL_2 = "general_2",
    SECONDARY_1 = "secondary_1",
    SECONDARY_2 = "secondary_2",
}

/** Размеры компонента SegmentedControl. */
export enum ESegmentedControlSize {
    SM = "sm",
    MD = "md",
    LG = "lg",
}
